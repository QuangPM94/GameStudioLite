# Test Fixtures

Place reusable malformed or relationship-focused PGS state fixtures here. Phase A builds its small invalid-severity payload in the test itself so the failure remains easy to understand.
